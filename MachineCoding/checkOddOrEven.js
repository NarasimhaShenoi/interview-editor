/**
 * 
    Print if the number is even or odd.

    No conditional statements to be used.
    No looping statements to be used.
 * 
 */

    function checkOddOrEven(num) {
        let a=["Even","Odd"];
        return a[num%2];
    }
    checkOddOrEven(2);
    checkOddOrEven(21);
